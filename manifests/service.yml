apiVersion: v1
kind: Service
metadata:
    name: rickwillemsepipelinesdotnetcoredocker
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: rickwillemsepipelinesdotnetcoredocker